<Window x:Class="ChatClient.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ChatClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="600"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Title -->
            <TextBlock Text="Select Mode" FontSize="24" FontWeight="Bold" Margin="10" HorizontalAlignment="Center" />

            <!-- Buttons -->
            <Button Width="200" Height="50" Margin="10" Content="Client" FontSize="18"
                    Style="{DynamicResource MaterialDesignFlatButton}"
                    Click="ClientButton_Click"/>
            <Button Width="200" Height="50" Margin="10" Content="Server" FontSize="18"
                    Style="{DynamicResource MaterialDesignFlatButton}"
                    Click="ServerButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
